{% extends 'base.html.twig' %}

{% block main %}

    <div class="container body-content span=8 offset=2">
        <div class="well">
            {{ form_start(artcileForm, {'action': path('article_create'), 'method': 'POST', 'multipart': true, 'attr': {'class': 'form-horizontal'} }) }}
                <fieldset>
                    <legend>Подай сигнал</legend>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="article_title">{{ form_label(artcileForm.title) }}</label>
                        <div class="col-sm-4">
                            {{ form_widget(artcileForm.title) }}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="article_image">{{ form_label(artcileForm.imageFile) }}</label>
                        <div class="col-sm-4 ">
                            {{ form_widget(artcileForm.imageFile) }}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="isFound">{{ form_label(artcileForm.isFound) }}</label>
                        <div class="col-sm-4 ">
                            {{ form_widget(artcileForm.isFound) }}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="article_town">{{ form_label(artcileForm.town) }}</label>
                        <div class="col-sm-4 ">
                            {{ form_widget(artcileForm.town) }}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="article_phone">{{ form_label(artcileForm.phone) }}</label>
                        <div class="col-sm-4 ">
                            {{ form_widget(artcileForm.phone) }}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="article_content">{{ form_label(artcileForm.content) }}</label>
                        <div class="col-sm-4">
                            {{ form_widget(artcileForm.content) }}
                        </div>
                    </div>

                    {{ form_row(artcileForm._token) }}

                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-4">
                            <a class="btn btn-default" href="{{ path('home_index') }}">Назад</a>
                            {{ form_widget(artcileForm.submit) }}
                        </div>
                    </div>
                </fieldset>
                {{ form_end(artcileForm) }}
        </div>
    </div>

{% endblock %}